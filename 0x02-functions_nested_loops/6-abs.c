#include "main.h"
/**
 * a function that computes the absolute value of an integer
 * prototype int _abs(int)
 * r is the variable
 */
int _abs(int r)
{
	if (r > 0)
	{
		return (r);
	}
	else 
		return (-r)
}
