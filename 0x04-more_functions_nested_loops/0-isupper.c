#include "main.h"
/**
 *_isupper - checks for uppercase character
 *@c : uppercase
 *Return : 1 if c is uppercase, otherwise 0
 */
int _isupper(int c)
{
	for (c = 'A'; c <= 'Z'; c++)

		if (c <= 'Z')
			return (1);
		else
			return (0);
}
