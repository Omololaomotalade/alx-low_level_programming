#include "main.h"
/**
 * int_islower - check the code for ALX School students.
 *
 * Return: 1 if c is lowercase otherwise 0.
 */
int_islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}

